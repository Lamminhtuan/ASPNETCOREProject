@model IEnumerable<FinalProject.Models.Voucher>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GiamGia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GiamGia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Voucher1">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Voucher1">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Voucher1">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
